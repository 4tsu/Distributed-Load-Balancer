#pragma once 
#include <vector>
#include "variables.hpp"


// =================================================

class Observer {
public:
    void export_cdview(std::vector<Atom> atoms);
};

// =================================================